{
  "name": "ai-dev-radar",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "test:openai": "node test-openai.js",
    "setup:check": "node -e \"console.log('Checking environment variables...'); const required = ['OPENAI_API_KEY', 'NEXT_PUBLIC_SUPABASE_URL', 'CRON_SECRET']; const missing = required.filter(key => !process.env[key]); if (missing.length) { console.error('❌ Missing:', missing.join(', ')); console.log('📝 Create .env.local file with these variables'); process.exit(1); } else { console.log('✅ All required environment variables are set!'); }\""
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "date-fns": "^3.0.0",
    "next": "^14.2.33",
    "openai": "^4.24.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-markdown": "^9.0.1"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "autoprefixer": "^10.0.1",
    "postcss": "^8",
    "tailwindcss": "^3.3.0",
    "typescript": "^5"
  }
}
